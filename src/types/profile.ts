// Interface Profile
export interface Profile {
    id: string;
    nickname: string;
    firstName: string;
    lastName: string;
    profilePicture: string | null; // Utilisez null si c'est acceptable
  }
  